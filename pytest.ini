[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
# Parallel execution by default (auto-detect cores)
# Browser selection handled by run-tests.sh to allow override
addopts = -n auto
